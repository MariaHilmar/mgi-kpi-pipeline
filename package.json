{
  "name": "mgi-pipeline",
  "private": true,
  "scripts": {
    "test": "python -m pytest",
    "dev": "npm run dev --prefix ../mgi-kpi-dashboard",
    "build": "npm run build --prefix ../mgi-kpi-dashboard",
    "start": "npm run start --prefix ../mgi-kpi-dashboard"
  }
}
